services:
  bot:
    build: .
    container_name: bikes-bot
    restart: unless-stopped
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - bot-logs:/app/logs

volumes:
  bot-logs:
